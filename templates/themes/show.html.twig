{% extends 'base.html.twig' %}

{% block title %}Themes{% endblock %}

{% block body %}
    <h1>Themes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ theme.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ theme.description | raw }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>
                    <img class="figure-img rounded " src="/images/uploads/themes/{{ theme.image }}" alt="{{ theme.image }}">
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_home') }}">Retour à l'accueil</a>

    <a href="{{ path('app_themes_edit', {'id': theme.id}) }}">edit</a>

    {{ include('themes/_delete_form.html.twig') }}
{% endblock %}
